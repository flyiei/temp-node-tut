/**
 * 
 * npm init 
 * npm init -y  --everything default
 * 
 * npm i <package name> -- local dependency for this particular project.
 * npm install -g <package name> --install global dependency.
 * sudo install -g <package name>
 * 
 */

ddddd
